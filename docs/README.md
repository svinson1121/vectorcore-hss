# VectorCore HSS  -- Documentation Index

| Document | Description |
|----------|-------------|
| [api.md](api.md) | OAM REST API reference  -- all endpoints, fields, and curl examples |

## Adding Documentation

Place new Markdown files in this directory. Update this index when you add a file.

**Rule:** Any change to `internal/api/` that adds, removes, or modifies an endpoint or field **must** be reflected in `docs/api.md` before the change is considered complete.
